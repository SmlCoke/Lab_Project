# Lab4 Task1 延迟测量自动化 - 快速开始

## 快速使用

### 1. 生成所有测试文件（一键生成）

```bash
cd /path/to/Lab_Project/DIC/Lab4/Task1
python3 generate_sp.py --fa-type both
```

这将在 `FA16/delay/` 和 `FA28/delay/` 目录下生成 48 个 HSPICE 测试文件。

### 2. 运行 HSPICE 仿真

**注意**: 需要在有 HSPICE 的环境（如 PowerShell）中运行

```bash
# 在 PowerShell 中运行所有仿真
python3 run_sp.py --base-dir .

# 或者只运行 FA16 的仿真
python3 run_sp.py --base-dir . --fa-type FA16

# 或者只运行 FA28 的仿真
python3 run_sp.py --base-dir . --fa-type FA28
```

### 3. 解析结果并生成统计报告

仿真完成后，运行：

```bash
python3 parse_delay.py .
```

这将生成：
- `delay_summary.csv` - 所有测试的详细延迟数据
- `delay_summary.xlsx` - Excel 格式的数据（便于分析）
- `delay_summary_stats.txt` - 统计摘要

## 一键运行示例

```bash
# 查看完整工作流程
./workflow_example.sh
```

## 测试模式说明

脚本自动生成 6 种路径 × 4 种静态输入组合 = 24 种测试：

### 测试的 6 种路径：
1. A → Sum
2. B → Sum  
3. Cin → Sum
4. A → Cout
5. B → Cout
6. Cin → Cout

### 每种路径的 4 种静态输入组合：
例如测试 A → Sum 时：
- B=0, Cin=0
- B=0, Cin=1
- B=1, Cin=0
- B=1, Cin=1

## 输出文件说明

### HSPICE 仿真生成的文件：
- `.sp` - HSPICE 输入脚本
- `.lis` - 仿真输出日志
- `.mt0` - 测量结果文件（被 parse_delay.py 解析）
- `.tr0`, `.ic0`, `.st0` 等 - 中间文件（可删除）

### 统计结果文件：
```
delay_summary_stats.txt 示例：

FA16:
  Path                 Min          Max          Avg          Count   
  --------------------------------------------------------------------
  A_to_Cout              25.30        35.40        30.20          4
  A_to_Sum               28.50        42.10        35.80          4
  ...
```

## 常见问题

### Q: HSPICE 命令找不到？
A: 确保在安装了 HSPICE 的环境中运行，并且 `hspice` 命令在 PATH 中。

### Q: 仿真运行很慢？
A: 48 个测试需要一定时间，可以用 `--fa-type` 参数分批运行：
```bash
python3 run_sp.py --base-dir . --fa-type FA16  # 先运行 FA16 (24个)
python3 run_sp.py --base-dir . --fa-type FA28  # 再运行 FA28 (24个)
```

### Q: 如何只运行部分测试？
A: 可以手动进入特定的 delay 目录，然后在该目录运行：
```bash
cd FA16/delay
hspice FA16_delay_A_to_Sum_B0_Cin0.sp > FA16_delay_A_to_Sum_B0_Cin0.lis
```

### Q: 需要安装什么 Python 包？
A: 需要 pandas 和 openpyxl：
```bash
pip install pandas openpyxl
```

## 与 Lab3 的对比

本脚本参考了 Lab3 的设计模式：
- `generate_sp.py` ← 参考 `Lab3/Task2/generate_sp.py`
- `run_sp.py` ← 参考 `Lab3/Task2/run_sp.py`  
- `parse_delay.py` ← 参考 `Lab3/parser_mt.py`

但针对 Lab4 的延迟测量需求进行了专门优化。

## 脚本特点

✓ 自动生成 48 个不同输入模式的测试文件  
✓ 批量运行 HSPICE 仿真  
✓ 自动解析 .mt0 文件提取延迟数据  
✓ 生成统计摘要（最小值、最大值、平均值）  
✓ 支持 CSV 和 Excel 格式输出  
✓ 完整的错误处理和进度显示

## 更多信息

详细说明请参考 `README.md`
